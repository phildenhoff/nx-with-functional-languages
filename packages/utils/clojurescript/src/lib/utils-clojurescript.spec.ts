import { utilsClojurescript } from './utils-clojurescript';

describe('utilsClojurescript', () => {
  it('should work', () => {
    expect(utilsClojurescript()).toEqual('utils-clojurescript');
  });
});
