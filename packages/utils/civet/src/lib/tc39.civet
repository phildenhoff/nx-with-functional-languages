export function sum(a: number, b: number): number
  a
  |> (x) => x + b

export function nameOfNumber(n: number): string
  switch n
    0
      "zero"
    1
      "one"
    else
      "something else"